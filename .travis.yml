language: python
sudo: false
python:
    - "2.7"
    - "3.5"
    - "3.6"
install:
    - pip install zc.buildout coverage coveralls
    - buildout bootstrap
    - buildout
script:
    - coverage run bin/test -v
after_success:
  - coveralls
notifications:
    email: false
cache:
  pip: true
  directories:
    - eggs/
